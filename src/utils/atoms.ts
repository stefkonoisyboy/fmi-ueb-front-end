import { atom } from "jotai";

export const searchCodeAtom = atom("");
export const searchNameAtom = atom("");
export const searchCategoryAtom = atom("Всички");
export const isLoadingAtom = atom(true);
export const secondsAtom = atom(1);
